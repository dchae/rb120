# The default return value of `to_s` is the object representation (Name of the object's class and object id) if the object does not have a defined `to_s` method.
# To be sure, we could inspect the class definition for the object to see if and how the `to_s` method is implemented.
