# The `self.class` reference on line 3 calls the class method on the original calling object.
# Since the original calling object is the Car object referenced by the `small_car` variable,
# the call to `self.class` will return the class of `small_car`, which is Car.
